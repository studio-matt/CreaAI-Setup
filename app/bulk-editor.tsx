'use client'; // Ensure this is a client component

import { BulkEditor } from '../components/bulk-editor'; // Adjust the import based on your structure

export default function BulkEditorPage() {
  return (
    <div>
      <BulkEditor />
    </div>
  );
}